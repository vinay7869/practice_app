import 'package:flutter/material.dart';

class Success3 extends StatelessWidget {
  final int index;
  const Success3({super.key, required this.index});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('3')),
    );
  }
}
