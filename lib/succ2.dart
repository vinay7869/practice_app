import 'package:flutter/material.dart';

class Success2 extends StatelessWidget {
  final int index;
  const Success2({super.key, required this.index});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('bhak')),
    );
  }
}
